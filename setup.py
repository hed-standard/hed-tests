"""
Minimal setup file for hed-tests repository.

This repository is primarily a test data repository, not a Python package.
This setup.py is retained only for compatibility with development tools.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
